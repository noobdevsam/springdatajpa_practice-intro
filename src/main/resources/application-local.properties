spring.datasource.username=${DB_USERNAME_B}
spring.datasource.password=${DB_USERPASS_B}
spring.datasource.url=${DB_URL_A}

spring.jpa.hibernate.ddl-auto=validate
spring.sql.init.mode=always

spring.flyway.user=${DB_USERNAME_A}
spring.flyway.password=${DB_USERPASS_A}
